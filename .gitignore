# Local working files — kept out of the repo.
datasheets/

# Build artefacts.
/kingfisher
/podprobe
target/
*.test

# Developer tool cache (golangci-lint installed via `make tools`).
/.bin/

# Editor / OS.
*.swp
.DS_Store
.idea/
