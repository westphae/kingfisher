[project]
name = "kingfisher-enclosures"
version = "0.1.0"
description = "CadQuery parametric models for kingfisher hub / pod enclosures"
requires-python = ">=3.12"
dependencies = [
    "cadquery==2.8.0",
]

[tool.uv]
package = false
